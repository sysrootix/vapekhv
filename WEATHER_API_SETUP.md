# Настройка погодного API

Система автоматически определяет плохие погодные условия в Хабаровске и увеличивает стоимость доставки на 20% при их наличии.

## Критерии плохих погодных условий:

1. **Экстремальный холод**: температура ниже -20°C
2. **Сильный дождь**: более 5 мм/ч
3. **Сильный снег**: более 2 мм/ч  
4. **Сильный ветер**: скорость более 15 м/с (54 км/ч)
5. **Шквалистый ветер**: порывы более 20 м/с
6. **Плохая видимость**: менее 500 метров
7. **Гроза**: любые грозовые явления
8. **Гололед**: при температуре от -2°C до 2°C с осадками
9. **Туман**: сильный туман, снижающий видимость

## Настройка API

### 1. Получение API ключа OpenWeatherMap

1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
2. Перейдите в раздел [API Keys](https://home.openweathermap.org/api_keys)
3. Создайте новый API ключ (бесплатный план дает 1000 запросов/день)

### 2. Установка переменной окружения

Добавьте в `.env` файл бэкенда:

```bash
WEATHER_API_KEY=ваш_api_ключ_здесь
```

Или используйте переменную `OPENWEATHER_API_KEY`:

```bash
OPENWEATHER_API_KEY=ваш_api_ключ_здесь
```

### 3. Перезапуск бэкенда

```bash
pm2 restart vapekhv-backend
```

## Ручное управление (fallback)

Если API ключ не установлен, система использует переменную `BAD_WEATHER`:

```bash
# Включить плохие погодные условия вручную
BAD_WEATHER=true

# Выключить
BAD_WEATHER=false
```

**Приоритет**: Если `BAD_WEATHER` установлена, она имеет приоритет над погодным API.

## Кеширование

Данные о погоде кешируются на 10 минут для оптимизации работы и снижения нагрузки на API.

## Логирование

Все запросы к погодному API логируются:
- Успешные запросы: `info` уровень
- Ошибки: `error` уровень
- Отключенный API: `warn` уровень

## Примеры работы

### При хорошей погоде:
- Температура: 5°C
- Дождь: 1 мм/ч
- Ветер: 8 м/с
- **Результат**: Доставка по обычным тарифам

### При плохой погоде:
- Температура: -25°C
- Снег: 3 мм/ч
- Ветер: 18 м/с
- **Результат**: Доставка +20% (например, 700₽ → 840₽)

### При экстремальных условиях:
- Гроза
- Сильный дождь: 8 мм/ч
- Ветер: 25 м/с
- **Результат**: Доставка +20% с несколькими причинами

## Технические детали

- **API**: OpenWeatherMap Current Weather API
- **Город**: Хабаровск (48.4827°N, 135.0838°E)
- **Единицы измерения**: метрические (градусы Цельсия, метры, м/с)
- **Таймаут запроса**: 5 секунд
- **Частота обновления**: каждые 10 минут (кеш)

